start (x) = {
  f, g = import "test/module.kopi"

  loop (x = x) => {
    print x
    command = input "(R)eload or (I)ncrement?"

    match command (
      "r" => start (x)
      "i" => (g x)
    )
  }
}

start 1
