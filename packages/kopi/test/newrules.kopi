print $ 1, (x => x), (a, b => a + b), [(1, 2), 1 + 1, (x => x), (a, b) => a + b]
print x => x

x = 1
f = () => x

print $ f ()

x = 2

print $ f ()

[]

print $ (a => b => a + b) 1 2

print $ (x, k => (x, k => k (x + 1)) (x + x, k)) (5, (x => x))

fix = f => (x => f (y => x x y)) x => f (y => x x y)

print $ (fix f => x => x * x) 5

print $ (date ()), (time ())

vecA, vecB = Vector (1, 2), Vector (2, 3)
print $ vecA + vecB | length | toFixed 3

print $ 1..2, "a".."z" | map (a, b) => a, b

print $ (1 + 1)..(f 5) | map (x) => x

print (name: "Joe", age: 20).name
