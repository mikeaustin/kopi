items = 1..3

ul = props => children => ("<ul>", children, "</ul>")
li = props => children => ("<li>", children, "</li>")


html = (
  ul () (items | map (item) => {
    li () item * item
  })
)

# html = (
#   ul () $ items | map (item) => {
#     li () item * item
#   }
# )

print html


html = (
  ul () (
    li () 1
    li () 2
    li () 3
  )
)

print html


# html = (
#   <ul>
#     {items.map(item => (
#       <li>{item * item}</li>
#     ))}
#   </ul>
# )
