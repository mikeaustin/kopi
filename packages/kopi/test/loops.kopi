xloop = (x) => {
  match (x % 100000) (
    0 => {
      print "Sleeping"
      sleep 1
    }
    _ => ()
  )
  print $ x | toLocaleString
  loop (x + 1)
}

# loop 1

loop exit => (x = 1) => {
  print $ x | toLocaleString
  sleep 0
  x + 1
}
