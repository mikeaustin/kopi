# methods = extend type 'String
#   capitalize: (this) => this ++ ", world!"
# )

extend String (
  greet: (this) => "Hello, " ++ this
)

extend String (
  wave: (this) => "Waves to " ++ this
)

print $ "Joe" | greet
print $ "Joe" | wave

{
  extend String (
    greet: (this) => "Welcome, " ++ this
  )

  print $ "Joe" | greet
}

print $ "Joe" | greet


extend Vector (
  fire: (this) => "Pew! Pew! Pew!"
)

print $ (Vector 10, 20) | fire

# Under the hood, it's just updating and using 'methods'
#
# methods = extend methods String (
#   greet: (this) => "Hello, " ++ this
# )
#
# methods.get("Joe".nativeConstructor).greet("Joe")
