extend String () (
  capitalize: (this) => () => 'toUpper this.(0..1) ++ this.(1..10)
)

print $ "foo" | capitalize

extend String (Iterable) ()

# extend Iterable () (
#   test
# )

inspect $ "abc" | _map (c) => 'succ c
inspect $ "12" | _map (a) => ("ab" | _map (b) => a ++ b)
inspect $ "12" | _flatMap (a) => "ab" | _map (b) => a ++ b
