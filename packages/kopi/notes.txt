fold (a, x => x * x), 1..5
sort "julie" ++ "moronuki"
xs | reduce 0 sum, x => sum + x


match n (
  0 => 1
  n => n * n
  n @ even => n * n
)

1..10 | map (n @ even n) => n * n

1..10 | map (n) @ even n => n * n

1..10 | map (n @ n > x) => n * n


scheme cond case


grayscale = color => match color (
  RGB (r, g, b) => (r + g + b) / 3
  HSL (h, s, l) => l
)


[1 + 1, "a" ++ "b", even 1, x => x]

[
  even $ 1,
  1 | toString,
  "a" ++ "b",
  even 1,
  x => x,
  (1, 2),
  1 + 1,
  1..5
]
